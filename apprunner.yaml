version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Building with Docker"
run:
  runtime-version: latest
  command: gunicorn --bind 0.0.0.0:8000 --workers 1 app:app
  network:
    port: 8000
    env: PORT